Replace "some.db" with appropriate path to run.  This project pulls data directly from Springboard server to run.
Results are the same for queries in folder "SQL" in this repo, this project simply adds the connection directly to the database.  
